# Production Environment Variables
# IMPORTANT: Never commit actual production values to Git!

# Database - Railway PostgreSQL URL
DATABASE_URL="postgresql://username:password@host:port/database?schema=public"

# JWT Configuration - GENERATE NEW SECRETS FOR PRODUCTION
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="GENERATE_A_STRONG_SECRET_HERE_128_CHARS"
JWT_EXPIRES_IN="7d"
REFRESH_TOKEN_SECRET="GENERATE_ANOTHER_STRONG_SECRET_HERE_128_CHARS"
REFRESH_TOKEN_EXPIRES_IN="30d"

# Server Configuration
NODE_ENV="production"
PORT="3000"

# Frontend URL - Your Vercel deployment URL
FRONTEND_URL="https://your-app.vercel.app"
ALLOWED_ORIGINS="https://your-app.vercel.app"

# Security Note:
# - Use strong, unique secrets for JWT tokens (128+ characters)
# - Never reuse secrets across environments
# - Rotate secrets regularly in production
# - Use Railway's environment variables interface to set these
